services:
  redis:
    image: redis:alpine
    env_file:
      - ".env"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"

volumes:
  redis_data: